const AvatarRange = {
  MIN: 1,
  MAX: 6
};

const LikesRange = {
  MIN: 15,
  MAX: 200
};

const CommentsRange = {
  MIN: 0,
  MAX: 30
};

const COMMENTS_STEP = 5;

export {AvatarRange, LikesRange, CommentsRange, COMMENTS_STEP};
